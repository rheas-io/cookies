import { Cookie } from '../src/cookie';

describe('Cookie', () => {
    /**
     *
     */
    it('toString', () => {
        const cookie = new Cookie('__session');
        //expect(cookie).toBe()
    });
});
